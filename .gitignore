# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Tauri / Rust
src-tauri/target
src-tauri/gen/

# Platform bundles (build outputs)
*.dmg
*.AppImage
*.deb
*.msi
*.exe

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment / secrets
.env
.env.*

# Sources config — contains API tokens. Copy sources.config.example.json to
# sources.config.json and fill in your tokens. The .example file is safe to commit.
sources.config.json

# API keys, certs, private keys
*.pem
*.key
*.p12
*.pfx
*.crt

# SQLite databases (runtime data, not source)
*.sqlite
*.db

# Whisper / ML model files (large binaries — download separately)
*.bin
*.gguf
*.ggml
